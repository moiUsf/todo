<h2><%= @item.title %></h2>
<p><%= @item.description %></p>

<%= link_to "Zurück", root_path %>
<%= link_to "Editieren", edit_item_path %>
<i class="fa fa-pencil-square-o" aria-hidden="false"></i>
<%= link_to "Löschen", item_path(@item), method: :delete, data: { confirm: "bist du sicher?"} %>